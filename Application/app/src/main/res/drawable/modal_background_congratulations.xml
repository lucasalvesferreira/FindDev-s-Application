<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/preto_2" /> <!-- Cor de fundo do card -->
    <stroke
        android:width="4dp"
        android:color="@color/verde_claro" /> <!-- Cor roxa da borda -->
    <corners android:radius="20dp" />
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="4dp"
        android:bottom="0dp" />
</shape>
