<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/black" /> <!-- Cor de fundo do card -->
    <stroke
        android:width="4dp"
        android:color="#660099" /> <!-- Cor roxa da borda -->
    <corners android:radius="8dp" />
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="4dp"
        android:bottom="0dp" />
</shape>
